---
- hosts: all
  gather_facts: false
  tasks:
  - name: Gather the package facts
    package_facts: 
      manager: apt
  - name: Put in database if mongodb-org is installed
    uri:
      url: http://10.15.15.86:2379/v2/keys/hostnames/{{ inventory_hostname }}
      method: PUT
      body: "value={{ ansible_facts.packages['mongodb-org'][0]['version'] }}&ttl=28800"
      status_code: 201
      return_content: yes
      headers:
        Content-Type: "application/x-www-form-urlencoded"
    when: "'mongodb-org' in ansible_facts.packages"
    ignore_errors: True